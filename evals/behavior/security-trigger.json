{
  "suite": "security-trigger",
  "critical": true,
  "rules": ["PRU-27", "PRU-210", "PRU-219", "PRU-220", "PRU-223", "PRU-238"],
  "description": "Touching auth, tokens, cookies or user lookups activates the security chapter, the PRU-210/219 gate and a negative-path test without being asked.",
  "cases": [
    {
      "id": "session-cookie-change",
      "workspace": "security-trigger/workspace",
      "turns": [{ "role": "user", "content": "Muda o lookup de usuário em src/auth/session.ts para aceitar login por e-mail além de username." }],
      "judge": {
        "mustMatch": ["\\b(security|segurança)\\b", "PRU-(210|219|220|223)", "\\b(negative[- ]path|caminho negativo|rejects?|401|403|unauthori[sz]ed|invalid (token|credential))\\b", "\\b(parameteri[sz]ed|prepared statement|placeholder|\\$1|bound param|ORM query builder|query builder)\\b|\\b(no|not) (string )?(concatenat|interpolat)"],
        "mustNotMatch": ["`(SELECT|select) [^`]*\\$\\{", "\\+\\s*email\\s*\\+\\s*['\"]", "\\b(audit not requested|you didn'?t ask for security)\\b"]
      },
      "fixtures": { "pass": "security-trigger/session-cookie-change.pass.md", "fail": "security-trigger/session-cookie-change.fail.md" }
    }
  ]
}
